---
phase: quick-260325-q1g
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(protected)/calendar/page.tsx
  - src/app/(protected)/board/page.tsx
  - src/app/(protected)/board/today/page.tsx
  - src/app/(protected)/print/tasks/page.tsx
  - src/app/(protected)/intake/page.tsx
  - src/app/(protected)/archived/page.tsx
  - src/app/(protected)/admin/pricing/page.tsx
  - src/components/board/order-detail-modal.tsx
  - src/components/auth/auth-guard.tsx
  - src/components/ui/loading-logo.tsx
autonomous: true
requirements: [MKT-71, MKT-116, MKT-117]
must_haves:
  truths:
    - "Every user-visible string in the application is in French"
    - "No hardcoded English text appears in toast messages, error states, loading indicators, or option labels"
    - "MKT-116 order form restructure confirmed complete (4-section flow with alteration/accessories split)"
    - "MKT-117 fabric items (Tissu au verge, Tissu au pied carre) are visible in the Accessories step"
  artifacts:
    - path: "src/app/(protected)/calendar/page.tsx"
      provides: "French toast messages, loading text, empty state"
    - path: "src/components/board/order-detail-modal.tsx"
      provides: "French option placeholder text"
    - path: "src/components/ui/loading-logo.tsx"
      provides: "French default loading text"
  key_links:
    - from: "All page components"
      to: "locales/fr.json"
      via: "useTranslations hook or direct French string replacement"
      pattern: "useTranslations|Chargement|Erreur"
---

<objective>
Complete the remaining MKT-71 (French translation), verify MKT-116 (order form restructure), and verify MKT-117 (fabric items) from the Hotte Design Platform Update Brief.

Purpose: MKT-116 and MKT-117 are already implemented in code (migrations 0043-0046, alteration-step.tsx, accessories-step.tsx). The remaining work is MKT-71: translating ~25 hardcoded English strings scattered across 10 files into French, plus a quick verification that fabric items appear correctly.

Output: All user-facing strings in French; verification notes for MKT-116 and MKT-117.
</objective>

<context>
@.planning/STATE.md
@.planning/quick/260322-sh4-fix-remaining-platform-update-brief-gaps/260322-sh4-SUMMARY.md
@src/app/(protected)/intake/page.tsx
@src/components/intake/accessories-step.tsx
@src/components/intake/alteration-step.tsx
@locales/fr.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all remaining hardcoded English strings with French (MKT-71)</name>
  <files>
    src/app/(protected)/calendar/page.tsx
    src/app/(protected)/board/page.tsx
    src/app/(protected)/board/today/page.tsx
    src/app/(protected)/print/tasks/page.tsx
    src/app/(protected)/intake/page.tsx
    src/app/(protected)/archived/page.tsx
    src/app/(protected)/admin/pricing/page.tsx
    src/components/board/order-detail-modal.tsx
    src/components/auth/auth-guard.tsx
    src/components/ui/loading-logo.tsx
  </files>
  <action>
Replace every remaining hardcoded English string visible to users with its French equivalent. Use DIRECT French string replacement (not i18n keys) for simple cases where the component does not already use useTranslations. For components already using useTranslations, add the translation key to locales/fr.json and locales/en.json if a suitable key does not exist.

Specific replacements (file-by-file):

**src/app/(protected)/calendar/page.tsx:**
- Line ~86: `'Failed to fetch orders'` -> `'Echec du chargement des commandes'`
- Line ~172: `toast.error('Please sign in as staff first')` -> `toast.error('Veuillez d\'abord vous connecter en tant que membre de l\'equipe')`
- Line ~186: `throw new Error('Failed to assign task')` -> `throw new Error('Echec de l\'attribution de la tache')`
- Line ~205: `toast.success('Task assigned to you')` -> `toast.success('Tache attribuee avec succes')`
- Line ~208: `toast.error('Failed to assign task')` -> `toast.error('Echec de l\'attribution de la tache')`
- Line ~218: `text="Loading calendar..."` -> `text="Chargement du calendrier..."`
- Line ~364: `No tasks due this week` -> `Aucune tache prevue cette semaine`

**src/app/(protected)/board/page.tsx:**
- Line ~145: `'Failed to fetch orders'` -> `'Echec du chargement des commandes'`
- Line ~192: `Failed to update order: ${response.status}` -> `Echec de mise a jour de la commande: ${response.status}`

**src/app/(protected)/board/today/page.tsx:**
- Line ~167: `'Failed to fetch'` -> `'Echec du chargement'`

**src/app/(protected)/print/tasks/page.tsx:**
- Line ~40: `'Failed to fetch'` -> `'Echec du chargement'`

**src/app/(protected)/intake/page.tsx:**
- Line ~148: `|| 'Service'` -> `|| 'Service'` (keep as-is, "Service" is the same in French)
- Line ~269: `'Failed to submit order'` -> `'Echec de soumission de la commande'`
- Line ~281: `'Failed to submit order'` -> `'Echec de soumission de la commande'`

**src/app/(protected)/archived/page.tsx:**
- Line ~62: `'Failed to fetch archived orders'` -> `'Echec du chargement des commandes archivees'`
- Line ~67: `'Failed to fetch archived orders'` -> `'Echec du chargement des commandes archivees'`

**src/app/(protected)/admin/pricing/page.tsx:**
- Line ~429: `<Badge variant='outline'>Price</Badge>` -> `<Badge variant='outline'>Prix</Badge>`

**src/components/board/order-detail-modal.tsx:**
- Line ~1399: `'Choose a service...'` -> `'Choisir un service...'`
- Line ~1473: `'Choose a garment type...'` -> `'Choisir un type de vetement...'`

**src/components/auth/auth-guard.tsx:**
- Line ~19: `text='Authenticating...'` -> `text='Authentification...'`

**src/components/ui/loading-logo.tsx:**
- Line ~98: default `text = 'Loading...'` -> `text = 'Chargement...'`
- Line ~114: default `text = 'Loading...'` -> `text = 'Chargement...'`

Use proper French characters with accents where possible (e, a with accents). For strings inside single-quoted JS where escaping is complex, use unaccented equivalents (standard practice in this codebase — see existing patterns).

DO NOT touch console.error/console.warn strings — those are developer-facing and stay English.
DO NOT touch throw new Error strings in catch blocks that are never shown to users.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && npx next build 2>&1 | tail -5</automated>
    After build passes, run: grep -rn --include='*.tsx' "toast\.\(error\|success\)\('[A-Z]" src/ | grep -v "Erreur\|Succès\|Membre\|requis\|Commande\|ajouté\|modifié\|supprimé\|archivé\|réactivé\|chargement\|connexion\|réessayer\|Veuillez\|Echec\|Tache\|Aucune\|Authentification\|Choisir" | grep -v node_modules
    Expect: zero matches (all English toasts replaced).
  </verify>
  <done>All user-facing hardcoded English strings replaced with French equivalents. Build passes. No English text visible in toasts, errors, loading states, or UI labels.</done>
</task>

<task type="auto">
  <name>Task 2: Verify MKT-116 and MKT-117 completeness</name>
  <files>
    (no modifications — verification only)
  </files>
  <action>
Verify the following are already complete and working by examining code (no changes needed):

**MKT-116 — Order Form Restructure:**
1. Confirm intake page.tsx has 4+ step flow: client -> pipeline -> alteration -> accessories -> pricing -> assignment
2. Confirm AlterationStep fetches only `category IN ('alterations', 'alteration')` services
3. Confirm AccessoriesStep fetches only `category IN ('accessories', 'accessory')` services
4. Confirm alteration services have editable `estimatedMinutes` time field (line ~525 and ~556 in alteration-step.tsx)
5. Confirm accessories have `isAccessory: true` flag and `estimatedMinutes: 0` (line ~339 in accessories-step.tsx)
6. Confirm API route.ts (line ~721) excludes `isAccessory` items from calendar logic
7. Confirm accessories support decimal qty (min 0.25, step 0.25 at line ~631-632 in accessories-step.tsx)

**MKT-117 — Fabric Items:**
1. Confirm migration 0044 seeds 'Tissu au verge' (unit: yard) and 'Tissu au pied carre' (unit: sq ft) with category='accessories'
2. Confirm AccessoriesStep query `.in('category', ['accessories', 'accessory'])` would pick these up
3. Confirm the price display shows `/ ${service.unit}` for fabric items (line ~602 in accessories-step.tsx)

If any gap is found, fix it. Otherwise output verification notes confirming completeness.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && grep -c "isAccessory.*true" src/components/intake/accessories-step.tsx && grep -c "estimatedMinutes" src/components/intake/alteration-step.tsx && grep -c "FABRIC_YARD\|FABRIC_SQFT" supabase/migrations/0044_seed_fabric_services.sql</automated>
    Expect: non-zero counts for all three greps confirming the code exists.
  </verify>
  <done>MKT-116 verified: 4-section form with alteration (time-based, calendar-feeding) and accessories (product-only, invoice-only, decimal qty) sections working correctly. MKT-117 verified: two fabric items seeded in migration 0044, visible in accessories step with unit-based pricing display.</done>
</task>

</tasks>

<verification>
1. `npx next build` passes (no TypeScript or lint errors from string changes)
2. `grep -rn "toast\.\(error\|success\)\('[A-Z]" src/ --include='*.tsx' | grep -v French-pattern` returns zero English toasts
3. `grep -rn "'Loading\.\.\.'\\|'Failed to\\|'Please \\|'No tasks\\|Choose a " src/ --include='*.tsx' | grep -v node_modules | grep -v console` returns zero hardcoded English
4. MKT-116 code structure confirmed via grep patterns
5. MKT-117 migration and rendering confirmed
</verification>

<success_criteria>
- Zero hardcoded English strings in user-visible UI (toasts, errors, loading states, labels, option text)
- Build compiles successfully
- MKT-116 order form has 4-section structure with alteration/accessories separation
- MKT-117 fabric items exist in seed migration and are queryable by accessories step
- Calendar logic only processes non-accessory (alteration) services
</success_criteria>

<output>
After completion, create `.planning/quick/260325-q1g-plan-remaining-mkt-116-mkt-71-mkt-117-re/260325-q1g-SUMMARY.md`
</output>
