---
phase: 09-responsive-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/intake/client-step.tsx
  - src/components/intake/garment-services-step.tsx
  - src/components/intake/pricing-step.tsx
  - src/app/board/page.tsx
  - src/components/board/order-card.tsx
  - src/app/globals.css
autonomous: false

must_haves:
  truths:
    - "All buttons and inputs are minimum 44px tall on mobile"
    - "Form fields stack vertically on iPhone (< 768px)"
    - "No horizontal scrolling on any page except kanban columns"
    - "All touch targets are easily tappable without precision"
    - "iOS input focus does not trigger page zoom"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Touch-target utilities and iOS zoom prevention"
      contains: "min-height: 44px"
      min_lines: 574
    - path: "src/components/navigation/mobile-bottom-nav.tsx"
      provides: "Mobile bottom navigation"
      exports: ["MobileBottomNav"]
    - path: "src/components/ui/modal.tsx"
      provides: "Full-screen modals on mobile"
      contains: "h-full md:h-auto"
  key_links:
    - from: "globals.css"
      to: "all buttons/inputs"
      via: "CSS media query @media (max-width: 767px)"
      pattern: "min-height: 44px"
    - from: "mobile-bottom-nav.tsx"
      to: "layout.tsx"
      via: "component import"
      pattern: "md:hidden"
    - from: "intake form components"
      to: "responsive grid classes"
      via: "Tailwind CSS"
      pattern: "grid-cols-1 sm:grid-cols-2"
---

<objective>
Audit existing responsive implementation and fix any gaps in button sizing, form stacking, and mobile layout.

Purpose: Most responsive work is ALREADY COMPLETE (mobile nav, full-screen modals, safe-area CSS, touch utilities exist). This plan verifies existing implementations meet all 8 requirements and identifies any missing responsive patterns.

Output: Verified responsive implementation with documented audit results and fixes for any identified gaps.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aymanbaig/Desktop/hottecouture-main/.planning/PROJECT.md
@/Users/aymanbaig/Desktop/hottecouture-main/.planning/ROADMAP.md
@/Users/aymanbaig/Desktop/hottecouture-main/.planning/STATE.md
@/Users/aymanbaig/Desktop/hottecouture-main/.planning/phases/09-responsive-design/09-RESEARCH.md

# Existing responsive infrastructure
@/Users/aymanbaig/Desktop/hottecouture-main/src/components/navigation/mobile-bottom-nav.tsx
@/Users/aymanbaig/Desktop/hottecouture-main/src/components/ui/modal.tsx
@/Users/aymanbaig/Desktop/hottecouture-main/src/app/globals.css
@/Users/aymanbaig/Desktop/hottecouture-main/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Audit touch-target compliance across intake forms</name>
  <files>
    src/components/intake/client-step.tsx
    src/components/intake/garment-services-step.tsx
    src/components/intake/pricing-step.tsx
  </files>
  <action>
    Audit all buttons, inputs, and interactive elements in intake forms for RES-03 compliance.

    For each component:
    1. Identify all interactive elements (buttons, inputs, select, textarea, clickable divs)
    2. Check if element has min-h-[44px] class OR inherits from globals.css media query
    3. For custom buttons (role="button"), verify min-h-[44px] is applied
    4. For form inputs, verify they inherit 44px from @media (max-width: 767px) rule in globals.css

    Fixes required if found:
    - Add `min-h-[44px]` to any button/link/clickable element < 44px
    - Add `touch-target` utility class to icon-only buttons
    - Verify quantity +/- buttons in garment-services-step are touch-friendly

    DO NOT recreate existing touch utilities - globals.css already has:
    - `.touch-target` class (lines 562-565)
    - `.touch-target-sm` class (lines 567-570)
    - Global button/input min-height rule (lines 574-587)

    Only ADD classes to elements that need them, don't redefine utilities.
  </action>
  <verify>
    grep -n "min-h-\[44px\]" src/components/intake/*.tsx
    grep -n "touch-target" src/components/intake/*.tsx
    npm run build
  </verify>
  <done>
    All buttons/inputs in intake forms have minimum 44px height on mobile via class or global rule.
  </done>
</task>

<task type="auto">
  <name>Verify and fix form field vertical stacking on mobile</name>
  <files>
    src/components/intake/client-step.tsx
    src/components/intake/garment-services-step.tsx
    src/components/intake/pricing-step.tsx
  </files>
  <action>
    Audit form layouts for RES-04 compliance (vertical stacking on mobile).

    For each component, find all grid layouts:
    1. Search for `grid grid-cols-` patterns
    2. Verify mobile-first pattern: `grid-cols-1` (default) then `sm:grid-cols-2` or `md:grid-cols-2`
    3. Check for hardcoded `grid-cols-2` or `grid-cols-3` without mobile variant

    Fix pattern:
    - `grid grid-cols-2` → `grid grid-cols-1 sm:grid-cols-2`
    - `grid grid-cols-3` → `grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3`
    - `flex flex-row` (for form fields) → `flex flex-col sm:flex-row`

    Verify spacing:
    - Use `gap-3` or `gap-4` for consistent spacing
    - Use `space-y-4` for vertical stacking on mobile

    DO NOT change layouts that are already mobile-first (grid-cols-1 sm:grid-cols-2 is correct).
  </action>
  <verify>
    grep -n "grid-cols-" src/components/intake/*.tsx
    grep -n "flex-row" src/components/intake/*.tsx
    npm run build
  </verify>
  <done>
    All form grids start at 1 column on mobile, expand to 2+ columns on sm/md breakpoints.
  </done>
</task>

<task type="auto">
  <name>Audit board page and order cards for mobile layout</name>
  <files>
    src/app/board/page.tsx
    src/components/board/order-card.tsx
  </files>
  <action>
    Verify mobile responsiveness on board page (kanban view).

    Board page audit (RES-01, RES-05):
    1. Check header stacks properly on mobile: `flex-col sm:flex-row`
    2. Verify filters hidden on mobile: `hidden md:flex` or similar
    3. Confirm kanban horizontal scroll exists (already implemented per research)
    4. Check padding: should be `px-4 sm:px-6` not hardcoded px-8

    Order card audit:
    1. Verify card width works on mobile (no fixed pixel widths)
    2. Check buttons are touch-friendly (min-h-[44px])
    3. Ensure card content doesn't overflow on 375px width (iPhone SE)

    Research confirms horizontal scroll kanban is ALREADY DONE - do not modify kanban scroll behavior.

    Fixes only if gaps found:
    - Add responsive classes to header layout
    - Fix any hardcoded padding values
    - Add min-h-[44px] to action buttons in cards
  </action>
  <verify>
    grep -n "flex-col" src/app/board/page.tsx
    grep -n "hidden md:" src/app/board/page.tsx
    grep -n "px-" src/app/board/page.tsx
    npm run build
  </verify>
  <done>
    Board page header stacks on mobile, filters hidden, kanban scrolls horizontally, cards fit 375px width.
  </done>
</task>

<task type="auto">
  <name>Add iPad portrait optimizations to globals.css</name>
  <files>
    src/app/globals.css
  </files>
  <action>
    Add iPad portrait media query for RES-07 compliance.

    Research shows custom media query needed for 768-1024px portrait range (lines 597-607 mentioned but verify exists).

    Check if this block exists:
    ```css
    @media (min-width: 768px) and (max-width: 1024px) and (orientation: portrait) {
      .container {
        padding-left: 1rem;
        padding-right: 1rem;
      }
      .kanban-column {
        min-width: 260px;
      }
    }
    ```

    If MISSING, add it after the existing mobile utilities (after line 607 area).
    If EXISTS, verify values and leave unchanged.

    DO NOT modify existing touch-target utilities or safe-area rules.
  </action>
  <verify>
    grep -A5 "orientation: portrait" src/app/globals.css
    npm run build
  </verify>
  <done>
    iPad portrait media query exists with container padding and kanban column width optimizations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Completed responsive audit and fixes:
    - Touch-target compliance (44px minimum) across intake forms
    - Form field vertical stacking on mobile (grid-cols-1 default)
    - Board page mobile layout verified
    - iPad portrait optimizations added to globals.css

    Existing responsive infrastructure verified:
    - Mobile bottom nav (RES-02) ✓
    - Full-screen modals (RES-06) ✓
    - Safe-area CSS ✓
    - Touch utilities ✓
  </what-built>
  <how-to-verify>
    Test on actual device or Safari DevTools:

    1. Open Safari DevTools → Responsive Design Mode
    2. Select "iPhone SE" (375px width - smallest case)
    3. Navigate to http://localhost:3000/intake

    **Client Step:**
    - [ ] All form inputs stack vertically (no side-by-side on iPhone)
    - [ ] All buttons are easily tappable without zooming
    - [ ] Tapping input does NOT zoom the page (16px font working)

    **Garment Services Step:**
    - [ ] Service cards stack in 1 column (not 2)
    - [ ] Quantity +/- buttons are touch-friendly
    - [ ] "Add to Order" button is 44px tall minimum

    **Pricing Step:**
    - [ ] Payment method buttons stack vertically
    - [ ] All inputs are tappable without precision

    4. Switch to "iPad" (820px width portrait)
    5. Navigate to http://localhost:3000/board

    **Board Page:**
    - [ ] Header stacks properly (no overflow)
    - [ ] Kanban columns scroll horizontally (not too wide)
    - [ ] Order cards readable and tappable
    - [ ] No horizontal page scroll (only kanban area scrolls)

    6. Test modal: Click any order card

    **Modal on iPhone:**
    - [ ] Modal takes full screen (edge-to-edge)
    - [ ] Close button accessible and tappable
    - [ ] Content scrolls independently

    **Modal on iPad:**
    - [ ] Modal is centered with max-width
    - [ ] Has rounded corners
    - [ ] Not full-screen
  </how-to-verify>
  <resume-signal>
    Type "approved" if all tests pass, OR describe specific issues found (e.g., "Client form fields still side-by-side on iPhone", "Close button too small on modal").
  </resume-signal>
</task>

</tasks>

<verification>
Automated checks:
- Build succeeds without TypeScript errors
- All intake forms have mobile-first grid layouts
- Touch-target classes applied where needed
- iPad portrait media query exists in globals.css

Manual verification (checkpoint):
- iPhone SE (375px): All forms usable, no zoom on input focus
- iPad portrait (820px): Board page fits without horizontal scroll
- Modals full-screen on iPhone, centered on iPad
- All touch targets easily tappable
</verification>

<success_criteria>
1. All buttons/inputs are minimum 44px tall on mobile
2. Form fields stack to 1 column on iPhone (< 768px)
3. No horizontal scrolling on any page except kanban columns
4. Board page works on iPad portrait without overflow
5. User confirms all workflows are usable on iPhone Safari
</success_criteria>

<output>
After completion, create `.planning/phases/09-responsive-design/09-01-SUMMARY.md` documenting:
- Audit results (what was already correct, what needed fixes)
- Fixes applied (specific files and changes)
- Verification results from human testing
- Any remaining gaps for Plan 02 (if needed)
</output>
