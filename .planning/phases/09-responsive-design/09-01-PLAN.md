# Phase 09-01: Mobile Navigation and Breakpoints

## Goal
Add mobile bottom navigation bar and ensure proper breakpoint handling across the app.

## Requirements Covered
- RES-01: Add responsive breakpoint for iPhone (< 768px)
- RES-02: Convert navigation to bottom tab bar on mobile

## Current State
- Breakpoints exist in tailwind.config.ts (xs: 475px, sm: 640px, md: 768px)
- No bottom nav component exists
- Header nav doesn't adapt for mobile

## Implementation

### Task 1: Create MobileBottomNav component
**File:** `src/components/navigation/mobile-bottom-nav.tsx`

```tsx
'use client';

import { usePathname } from 'next/navigation';
import Link from 'next/link';
import { Home, ClipboardList, Users, Calendar, Settings } from 'lucide-react';
import { cn } from '@/lib/utils';

const navItems = [
  { href: '/board', icon: Home, label: 'Board' },
  { href: '/intake', icon: ClipboardList, label: 'Intake' },
  { href: '/clients', icon: Users, label: 'Clients' },
  { href: '/calendar', icon: Calendar, label: 'Calendar' },
];

export function MobileBottomNav() {
  const pathname = usePathname();

  return (
    <nav className="fixed bottom-0 left-0 right-0 z-50 bg-white border-t border-border md:hidden">
      <div className="flex justify-around items-center h-16 px-2 safe-area-inset-bottom">
        {navItems.map((item) => {
          const isActive = pathname.startsWith(item.href);
          return (
            <Link
              key={item.href}
              href={item.href}
              className={cn(
                "flex flex-col items-center justify-center flex-1 py-2 min-h-[44px]",
                "text-xs font-medium transition-colors",
                isActive
                  ? "text-primary-600"
                  : "text-muted-foreground hover:text-foreground"
              )}
            >
              <item.icon className={cn("w-6 h-6 mb-1", isActive && "text-primary-600")} />
              <span>{item.label}</span>
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```

### Task 2: Update layout.tsx
**File:** `src/app/layout.tsx`

Add MobileBottomNav and adjust main content padding:
- Import MobileBottomNav
- Add `pb-16 md:pb-0` to main content area for bottom nav spacing
- Hide header nav items on mobile: `hidden md:flex`

### Task 3: Add safe-area CSS
**File:** `src/app/globals.css`

Add safe area insets for iPhone notch/home indicator:
```css
.safe-area-inset-bottom {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}
```

## Verification
1. On mobile (< 768px): Bottom nav visible with 4 tabs
2. On desktop (>= 768px): Bottom nav hidden, header nav visible
3. Active tab highlighted correctly
4. Touch targets are 44px minimum height
5. Safe area respected on iPhone X+
