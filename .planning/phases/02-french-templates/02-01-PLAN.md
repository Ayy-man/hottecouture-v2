---
phase: 02-french-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/intake/order-summary.tsx
  - src/app/(protected)/track/[id]/page.tsx
autonomous: true
requirements:
  - BUG-3

must_haves:
  truths:
    - "Order summary screen shows all text in French after order creation"
    - "Track page shows correct shop phone number from env var, not hardcoded (514) 855-1234"
    - "No English strings visible to customers on order-summary or track pages"
  artifacts:
    - path: "src/components/intake/order-summary.tsx"
      provides: "French order confirmation screen"
      contains: "Commande créée avec succès"
    - path: "src/app/(protected)/track/[id]/page.tsx"
      provides: "Env var phone number on track page"
      contains: "NEXT_PUBLIC_SHOP_PHONE"
  key_links:
    - from: "src/app/(protected)/track/[id]/page.tsx"
      to: "NEXT_PUBLIC_SHOP_PHONE env var"
      via: "process.env fallback pattern"
      pattern: "process\\.env\\.NEXT_PUBLIC_SHOP_PHONE.*514-667-0082"
---

<objective>
Replace all remaining English customer-facing strings with French equivalents in order-summary.tsx (~20 strings) and fix the hardcoded phone number in track/[id]/page.tsx to use the NEXT_PUBLIC_SHOP_PHONE env var.

Purpose: BUG-3 requires all customer-facing text to be in French for the Quebec client. Research audit confirmed these are the only two files with remaining English customer-facing strings — all other surfaces (portal, status, booking, SMS/GHL templates, invoices) are already French.

Output: Two modified files with all customer-visible English replaced by French, and phone number using env var pattern.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-M2.md
@.planning/STATE.md
@.planning/phases/02-french-templates/02-RESEARCH.md
@.planning/phases/02-french-templates/02-VALIDATION.md

<interfaces>
<!-- Reference implementation: portal page env var phone pattern -->
From src/app/(public)/portal/page.tsx:
```tsx
const SHOP_PHONE = process.env.NEXT_PUBLIC_SHOP_PHONE || '514-667-0082';
// ...
<a href={`tel:${SHOP_PHONE.replace(/-/g, '')}`} className='underline text-primary-600 hover:text-primary-700'>{SHOP_PHONE}</a>
```

<!-- order-summary.tsx imports (no changes needed) -->
From src/components/intake/order-summary.tsx:
```tsx
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { IntakeResponse } from '@/lib/dto';
import { formatCurrency } from '@/lib/pricing/client';
import { HLogo } from '@/components/ui/h-logo';
```
</interfaces>

CONVENTION: This project uses HARDCODED French strings (not useTranslations). See STATE.md decisions from Phases 19, 22-01, 24-02. Do NOT introduce useTranslations() calls — that is Phase 10 (MKT-71) scope.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all English strings in order-summary.tsx with French</name>
  <files>src/components/intake/order-summary.tsx</files>
  <read_first>
    - src/components/intake/order-summary.tsx (current file with English strings)
    - src/app/(public)/portal/page.tsx (reference for French string style)
    - .planning/phases/02-french-templates/02-RESEARCH.md (full string mapping table)
  </read_first>
  <action>
Replace every English string in order-summary.tsx with French equivalents. Use hardcoded French strings (project convention — do NOT use useTranslations). The commented-out `// const t = useTranslations('intake.submit')` line should remain as-is (Phase 10 concern).

Complete string replacement map (every English string and its exact French replacement):

1. Line 31 "No order data available" -> "Aucune donnee de commande disponible"
2. Line 76 "Order Created Successfully!" -> "Commande creee avec succes !"
3. Line 79 "Order #{order.orderNumber} has been created successfully" -> "La commande #{order.orderNumber} a ete creee avec succes"
4. Line 88 CardTitle "Order Details" -> "Details de la commande"
5. Line 89-90 CardDescription "Order #{order.orderNumber} - {date}" -> "Commande #{order.orderNumber} - {date}"
6. Line 97 h3 "Order Information" -> "Informations de la commande"
7. Line 102 "Order Number:" -> "Numero de commande :"
8. Line 109 "Order ID:" -> "Identifiant :"
9. Line 113 "Status:" -> "Statut :"
10. Line 115 Badge "Pending" -> "En attente"
11. Line 122 h3 "Pricing Summary" -> "Resume des prix"
12. Line 125 "Subtotal:" -> "Sous-total :"
13. Line 132 "Rush Fee:" -> "Frais express :"
14. Line 143 "TPS: Canada tax" -> "TPS (taxe federale)"
15. Line 151 "TVQ: Quebec tax" -> "TVQ (taxe provinciale)"
16. Line 159 "Tax:" -> "Taxe :"
17. Line 167 "Total:" -> "Total :"
18. Line 183 CardTitle "Order QR Code" -> "Code QR de la commande"
19. Line 184-185 CardDescription "Use this QR code to track the order" -> "Utilisez ce code QR pour suivre la commande"
20. Line 192 img alt "Order QR Code" -> "Code QR de la commande"
21. Line 194-195 "Order #{order.orderNumber}" -> "Commande #{order.orderNumber}"
22. Line 208 Button "Print Labels" -> "Imprimer les etiquettes"
23. Line 213 Button "New Order" -> "Nouvelle commande"
24. Line 222 CardTitle "What's Next?" -> "Prochaines etapes"
25. Line 231 h4 "Print Labels" -> "Imprimer les etiquettes"
26. Line 232-235 paragraph "Print labels for each garment to track them through the process" -> "Imprimez les etiquettes pour chaque vetement afin de les suivre tout au long du processus"
27. Line 244 h4 "Start Work" -> "Commencer le travail"
28. Line 245-247 paragraph "Begin working on the garments according to the selected services" -> "Commencez a travailler sur les vetements selon les services selectionnes"
29. Line 255 h4 "Update Status" -> "Mettre a jour le statut"
30. Line 256-257 paragraph "Update the order status as work progresses" -> "Mettez a jour le statut de la commande au fur et a mesure de l'avancement"

IMPORTANT: Use proper French characters with accents in the actual JSX: e with accent aigu (e), e with accent grave (e), a with accent grave (a), c with cedilla (c). The map above is ASCII-only for readability but the code MUST use the Unicode characters. Specifically:
- "creee" should be "creee" with accents: "cr\u00e9\u00e9e"
- "succes" should have accent: "succ\u00e8s"
- "Details" should have accent: "D\u00e9tails"
- "Resume" should have accents: "R\u00e9sum\u00e9"
- "Numero" should have accent: "Num\u00e9ro"
- "federale" should have accent: "f\u00e9d\u00e9rale"
- "etiquettes" should have accent: "\u00e9tiquettes"
- "etapes" should have accent: "\u00e9tapes"
- "vetement/vetements" should have circumflex: "v\u00eatement/v\u00eatements"
- "selectionnees/selectionnes" should have accents: "s\u00e9lectionn\u00e9s"

Refer to the research file's "Order Summary -- Full String Map" section for the authoritative accent-correct French strings.

Do NOT change: imports, component structure, CSS classes, props, TypeScript types, or any logic. Only change string content.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - order-summary.tsx contains "Commande cr" (accent e-e-e with "avec succ")
    - order-summary.tsx contains "Aucune donn"
    - order-summary.tsx contains "En attente" (not "Pending")
    - order-summary.tsx contains "Sous-total"
    - order-summary.tsx contains "Frais express"
    - order-summary.tsx contains "TPS (taxe f"
    - order-summary.tsx contains "TVQ (taxe provinciale)"
    - order-summary.tsx contains "Code QR de la commande"
    - order-summary.tsx contains "Imprimer les"
    - order-summary.tsx contains "Nouvelle commande"
    - order-summary.tsx contains "Prochaines"
    - order-summary.tsx contains "Commencer le travail"
    - order-summary.tsx contains "Mettre"
    - order-summary.tsx does NOT contain "Order Created Successfully"
    - order-summary.tsx does NOT contain "What's Next"
    - order-summary.tsx does NOT contain "Print Labels"
    - order-summary.tsx does NOT contain "New Order"
    - order-summary.tsx does NOT contain "Start Work"
    - order-summary.tsx does NOT contain "Update Status"
    - order-summary.tsx does NOT contain "Pricing Summary"
    - order-summary.tsx does NOT contain "Order Information"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>All ~20 English strings in order-summary.tsx replaced with French equivalents using hardcoded strings (project convention). TypeScript compiles clean. No useTranslations() introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded phone number in track page with env var</name>
  <files>src/app/(protected)/track/[id]/page.tsx</files>
  <read_first>
    - src/app/(protected)/track/[id]/page.tsx (current file with hardcoded phone)
    - src/app/(public)/portal/page.tsx (reference implementation of SHOP_PHONE env var pattern)
  </read_first>
  <action>
Replace the hardcoded phone number `(514) 855-1234` with the `NEXT_PUBLIC_SHOP_PHONE` env var pattern, matching the portal page implementation exactly.

Specific changes:

1. Add this line near the top of the `TrackOrderPage` component function body (after the state declarations, before the useEffect):
```tsx
const SHOP_PHONE = process.env.NEXT_PUBLIC_SHOP_PHONE || '514-667-0082';
```

2. Replace the contact section at the bottom (currently around line 206-211):

BEFORE:
```tsx
<p className='text-sm text-muted-foreground'>
  Des questions? Contactez-nous au{' '}
  <a href='tel:+15148551234' className='text-primary-600 font-medium hover:underline'>
    (514) 855-1234
  </a>
</p>
```

AFTER:
```tsx
<p className='text-sm text-muted-foreground'>
  Des questions? Contactez-nous au{' '}
  <a href={`tel:${SHOP_PHONE.replace(/-/g, '')}`} className='text-primary-600 font-medium hover:underline'>
    {SHOP_PHONE}
  </a>
</p>
```

This removes the wrong hardcoded phone number and uses the same env var with the same `514-667-0082` fallback as the portal page.

Do NOT change: any other part of the file (status labels, dates, garments section, imports, types). Only the phone number section.
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - track/[id]/page.tsx contains "NEXT_PUBLIC_SHOP_PHONE"
    - track/[id]/page.tsx contains "514-667-0082" (fallback value)
    - track/[id]/page.tsx contains "SHOP_PHONE.replace(/-/g, '')" (tel: href pattern)
    - track/[id]/page.tsx contains "{SHOP_PHONE}" (display value in JSX)
    - track/[id]/page.tsx does NOT contain "855-1234"
    - track/[id]/page.tsx does NOT contain "+15148551234"
    - track/[id]/page.tsx does NOT contain "(514) 855"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Track page phone number uses NEXT_PUBLIC_SHOP_PHONE env var with 514-667-0082 fallback, matching portal page pattern. Hardcoded (514) 855-1234 removed. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles clean (no type errors from string changes)
2. `npx next build` — Full build passes
3. Grep confirms no remaining English customer-facing strings in either file:
   - `grep -c "Order Created\|Print Labels\|New Order\|What's Next\|Start Work\|Update Status\|Pricing Summary\|Order Information" src/components/intake/order-summary.tsx` returns 0
   - `grep -c "855-1234" src/app/\(protected\)/track/\[id\]/page.tsx` returns 0
4. Grep confirms French strings present:
   - `grep -c "Commande cr" src/components/intake/order-summary.tsx` returns >= 1
   - `grep -c "NEXT_PUBLIC_SHOP_PHONE" src/app/\(protected\)/track/\[id\]/page.tsx` returns >= 1
</verification>

<success_criteria>
- All ~20 English strings in order-summary.tsx replaced with hardcoded French
- Track page phone number uses NEXT_PUBLIC_SHOP_PHONE env var (not hardcoded)
- TypeScript compiles clean
- No useTranslations() introduced (Phase 10 concern)
- No changes to component logic, imports, or styling
</success_criteria>

<output>
After completion, create `.planning/phases/02-french-templates/02-01-SUMMARY.md`
</output>
