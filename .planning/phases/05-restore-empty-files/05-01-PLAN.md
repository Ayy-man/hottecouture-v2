---
phase: 05-restore-empty-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/orders/route.ts
autonomous: true
requirements:
  - INFRA-1

must_haves:
  truths:
    - "GET /api/orders endpoint returns order data (not 404 or empty response)"
    - "Seamstress filtering via ?seamstressId= query param works (Phase 27 RBAC preserved)"
    - "Board, calendar, today view, workload, print/tasks, client detail, and archive pages can fetch orders"
    - "TypeScript compiles without errors"
  artifacts:
    - path: "src/app/api/orders/route.ts"
      provides: "GET /api/orders endpoint with RPC-based order fetching and seamstress filtering"
      min_lines: 100
      exports: ["GET", "dynamic", "revalidate", "fetchCache"]
      contains: "get_orders_with_details"
  key_links:
    - from: "src/lib/board/useBoardData.ts"
      to: "/api/orders"
      via: "fetch call with seamstressId query param"
      pattern: "api/orders"
    - from: "src/app/(protected)/board/page.tsx"
      to: "src/lib/board/useBoardData.ts"
      via: "useBoardData hook"
      pattern: "useBoardData"
    - from: "src/app/api/orders/route.ts"
      to: "supabase.rpc('get_orders_with_details')"
      via: "Supabase RPC call"
      pattern: "get_orders_with_details"
---

<objective>
Restore the single empty file `src/app/api/orders/route.ts` from git commit `0f76a39` and verify the app compiles cleanly.

Purpose: This API route is the primary endpoint for fetching all non-archived orders. It is consumed by 7+ pages/components (board, calendar, today view, workload, print/tasks, client detail, archive). While empty, all these pages return no order data at runtime.

Output: A fully restored 120-line `src/app/api/orders/route.ts` with Phase 27 seamstress RBAC filtering intact. Clean TypeScript compilation.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP-M2.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-restore-empty-files/05-RESEARCH.md
@.planning/phases/05-restore-empty-files/05-VALIDATION.md

File consumers (13 files reference /api/orders):
- src/lib/board/useBoardData.ts (primary data hook)
- src/app/(protected)/board/page.tsx
- src/app/(protected)/board/today/page.tsx
- src/app/(protected)/board/workload/page.tsx
- src/app/(protected)/calendar/page.tsx
- src/app/(protected)/print/tasks/page.tsx
- src/app/(protected)/clients/[id]/page.tsx
- src/app/(protected)/archived/page.tsx
- src/app/(protected)/status/page.tsx
- src/app/(protected)/orders/history/page.tsx
- src/components/board/order-detail-modal.tsx
- src/components/board/archive-button.tsx
- src/app/api/orders/[id]/measurements/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore src/app/api/orders/route.ts from commit 0f76a39</name>
  <files>src/app/api/orders/route.ts</files>
  <read_first>
    - src/app/api/orders/route.ts (currently 0 bytes — confirm empty before restoring)
    - src/lib/board/useBoardData.ts (primary consumer — understand how this endpoint is called)
  </read_first>
  <action>
Restore the file using git checkout from the last known-good commit:

```bash
git checkout 0f76a39 -- src/app/api/orders/route.ts
```

This restores the 120-line file that provides:
- `export const dynamic = 'force-dynamic'` (line 1)
- `export const revalidate = 0` (line 2)
- `export const fetchCache = 'force-no-store'` (line 3)
- `import { NextResponse } from 'next/server'` (line 5)
- `import { createServiceRoleClient } from '@/lib/supabase/server'` (line 6)
- `export async function GET(request: Request)` handler (line 8-120)

The GET handler:
- Parses query params: `page`, `limit`, `client_id`, `seamstressId`
- Creates Supabase service role client
- Calls `supabase.rpc('get_orders_with_details', { p_limit, p_offset, p_client_id, p_assigned_seamstress_id })` for Phase 27 RBAC filtering
- Returns JSON: `{ success, orders, pagination, timestamp }`

CRITICAL: Do NOT use the remote `claude` branch version (commit `bbd4871`). That version uses simple `supabase.from('order').select('*')` without seamstress filtering, which would break Phase 27 RBAC.

After restoration, verify the file is staged by git (git checkout from a commit auto-stages).
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && wc -c src/app/api/orders/route.ts | grep -v "^       0" && grep "get_orders_with_details" src/app/api/orders/route.ts && grep "seamstressId" src/app/api/orders/route.ts && echo "RESTORE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `wc -c src/app/api/orders/route.ts` outputs a number greater than 4000 (not 0)
    - `grep 'get_orders_with_details' src/app/api/orders/route.ts` finds a match (Phase 27 RPC call present)
    - `grep 'seamstressId' src/app/api/orders/route.ts` finds a match (RBAC query param present)
    - `grep 'export async function GET' src/app/api/orders/route.ts` finds a match (route handler exported)
    - `grep "export const dynamic = 'force-dynamic'" src/app/api/orders/route.ts` finds a match (cache bypass)
    - `git status` shows `src/app/api/orders/route.ts` as modified/staged
  </acceptance_criteria>
  <done>src/app/api/orders/route.ts is restored to its 120-line Phase 27 version with get_orders_with_details RPC and seamstressId filtering. File is non-empty and staged in git.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation and confirm no other empty files</name>
  <files>src/app/api/orders/route.ts</files>
  <read_first>
    - src/app/api/orders/route.ts (verify restored content is correct after Task 1)
  </read_first>
  <action>
Run three verification steps:

1. Confirm no other empty source files exist:
```bash
find src -type f -empty
```
Expected output: no files listed (the only empty file was just restored).

2. Run TypeScript compilation check:
```bash
npx tsc --noEmit
```
Expected output: exits with code 0, no type errors. If errors appear, they are pre-existing and unrelated to this restoration (the empty file was a valid — though useless — TypeScript file that exported nothing).

3. Verify the restored file line count matches expectations:
```bash
wc -l src/app/api/orders/route.ts
```
Expected output: 120 lines.

If `tsc --noEmit` produces errors, inspect them. If they are in OTHER files (not src/app/api/orders/route.ts), note them in the summary but do not fix them — they are pre-existing and out of scope for INFRA-1.

After verification passes, commit the restoration:
```bash
git add src/app/api/orders/route.ts
git commit -m "restore: add back api/orders/route.ts content accidentally emptied in 563d2cd

The file was emptied to 0 bytes during the sync commit (563d2cd). This restores
it from commit 0f76a39, which includes the Phase 27 seamstress RBAC filtering
via get_orders_with_details RPC.

INFRA-1

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>"
```
  </action>
  <verify>
    <automated>cd /Users/aymanbaig/Desktop/Manual\ Library.noSync/hottecouture-main && find src -type f -empty | wc -l | grep "^       0$" && echo "NO EMPTY FILES" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `find src -type f -empty` returns 0 results (no empty source files remain)
    - `npx tsc --noEmit` exits cleanly OR any errors are in files OTHER than src/app/api/orders/route.ts
    - `wc -l src/app/api/orders/route.ts` outputs 120
    - `git log --oneline -1` shows a commit message containing "restore" and "api/orders/route.ts"
  </acceptance_criteria>
  <done>Zero empty source files remain. TypeScript compiles without errors related to the restored file. Restoration committed to git with descriptive message referencing INFRA-1 and the source commit 0f76a39.</done>
</task>

</tasks>

<verification>
1. `test -s src/app/api/orders/route.ts` — file exists and is non-empty
2. `grep 'get_orders_with_details' src/app/api/orders/route.ts` — Phase 27 RPC present
3. `grep 'seamstressId' src/app/api/orders/route.ts` — RBAC filtering param present
4. `find src -type f -empty | wc -l` — returns 0 (no empty files)
5. `npx tsc --noEmit` — TypeScript compiles clean (or errors are pre-existing in other files)
6. `wc -l src/app/api/orders/route.ts` — outputs 120
</verification>

<success_criteria>
- src/app/api/orders/route.ts restored to 120 lines from commit 0f76a39
- File contains get_orders_with_details RPC call (Phase 27 seamstress filtering)
- File contains seamstressId query param handling
- No empty source files remain in src/
- TypeScript compiles without errors in the restored file
- Change committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/05-restore-empty-files/05-01-SUMMARY.md`
</output>
